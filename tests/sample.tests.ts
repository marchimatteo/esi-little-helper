import { Esi } from '../lib';

describe('calculate', function() {
  it('add', function() {
    let result = 7;
    expect(result).toBe(7);
  });

  test('random', async () => {
    let esi = new Esi();
  })
});
